.PHONY: run

# GOWORK=off: this example is a self-contained leaf module that resolves the
# in-repo core and Mantle modules through replace directives rather than the
# repository workspace.
run:
	GOWORK=off go run . $(PROMPT)
